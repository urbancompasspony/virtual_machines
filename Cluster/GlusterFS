
nano /etc/hosts
Add the following lines:
192.168.10.10 HOSTNAMEHERE1
192.168.10.11 HOSTNAMEHERE2

sudo apt install glusterfs-server glusterfs-client -y

sudo systemctl start glusterd
sudo systemctl enable glusterd

sudo systemctl status glusterd

$ sudo gluster peer probe HOSTNAMEHERE1
$ sudo gluster peer probe HOSTNAMEHERE2

$ sudo gluster peer status

Both Nodes
$ sudo mkdir /glustervolume/vol1

$ sudo gluster volume create vol1 replica 2 node1:/glustervolume/vol1 node2:/glustervolume/vol1

$ sudo gluster volume start vol1

$ sudo gluster volume status

/etc/fstab
localhost:/vol1 /opt glusterfs defaults,_netdev 0 0

